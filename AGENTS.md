# tsuite_deploy 代理执行说明

## 适用范围

本文档适用于 `tsuite_deploy` 部署仓库。

## 部署任务入口

- 执行镜像发布、部署控制机维护、临时支持接入、客户部署、升级、验证或回退前，必须完整阅读
  [AI Agent 部署运行手册](docs/AI_AGENT_DEPLOYMENT_RUNBOOK.md)。
- 手册中的“当前生产拓扑”和“不可违反的边界”是部署任务的默认事实来源；若现场状态与手册不符，
  先以只读命令核实并更新手册，不得直接猜测或覆盖现场配置。
- 部署任务只有在目标镜像实际运行、必要迁移成功、健康检查通过且临时支持会话已关闭后才算完成。

## 共享组件版本

- 本地应用可以通过 `file:` 引用同一工作区内的共享组件库，便于开发和联调。
- GitHub Actions 与 Docker 发布构建必须通过不可变 Git Tag 检出 `tai-chat`、`transinfo-ui` 等共享组件库。
- 发布构建不得使用 `file:`、分支名或裸提交 SHA 作为共享组件版本。
- Tag 必须与组件库 `package.json` 版本一致，格式为 `v<version>`；修改构建默认版本前应确认对应远端 Tag 已存在。
- 已用于发布的 Tag 不得移动或覆盖；组件内容变化时应先发布新组件版本，再更新构建输入。

## 发布验证

- 修改共享组件 Tag 后，至少执行工作流静态检查，并通过一次相关镜像构建验证组件检出与前端编译。
- 当任务包含镜像构建与部署时，二者构成同一交付链路：不得在镜像构建、推送、目标节点部署、数据库迁移或部署后健康验证尚未完成时结束会话或宣称完成。应持续跟进至目标镜像实际运行、必要迁移成功且相关健康检查通过；若被外部凭据或基础设施阻塞，须明确报告具体阻塞点和当前运行状态。
- 不要在工作流或文档中写入源码 Token、GHCR Token 或其他密钥值。
