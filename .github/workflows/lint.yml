name: Lint

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  shell:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install shell tools
        run: sudo apt-get update && sudo apt-get install -y python3-yaml shellcheck

      - name: Check shell syntax
        run: bash -n single-node/deploy.sh install.sh multi-node/tools/create-source-bundle.sh

      - name: Run ShellCheck
        run: shellcheck single-node/deploy.sh install.sh multi-node/tools/create-source-bundle.sh tests/test_static.sh

      - name: Run static tests
        run: tests/test_static.sh
