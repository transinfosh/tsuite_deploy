CUSTOM_IMAGE={{ frappe_image.rsplit(':', 1)[0] }}
CUSTOM_TAG={{ frappe_image.rsplit(':', 1)[1] }}
ERPNEXT_VERSION={{ frappe_image.rsplit(':', 1)[1] }}
PULL_POLICY={{ frappe_pull_policy }}
RESTART_POLICY=unless-stopped
DB_PASSWORD={{ frappe_db_password }}
